name: Deploy

on:
  workflow_dispatch:
    inputs:
      provider:
        description: 'Cloud provider'
        default: 'vultr'
        type: choice
        options:
          - vultr
          - hetzner
      deployment:
        description: 'Deployment to deploy'
        default: 'default'
        type: choice
        options:
          - default
          - ez3proxy
          - nixginx
          - k3s

env:
  VULTR_API_KEY: ${{ inputs.provider == 'vultr' && secrets.VULTR_API_KEY || '' }}
  HCLOUD_TOKEN: ${{ inputs.provider == 'hetzner' && secrets.HCLOUD_TOKEN || '' }}
  TF_VAR_encryption_passphrase: ${{ secrets.TF_ENCRYPTION_PASSPHRASE }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Setup SSH key
        run: |
          mkdir -p keys
          echo "${{ secrets.DEPLOY_KEY }}" > keys/deploy-key
          chmod 600 keys/deploy-key

      # Deploy infrastructure
      - run: nix develop --command just provider=${{ inputs.provider }} deploy ${{ inputs.deployment }}

      # Write server IP to deployment config
      - run: nix develop --command just provider=${{ inputs.provider }} write-config ${{ inputs.deployment }}

      # Commit state + server config
      - name: Commit state and config
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add terraform/ deployments/${{ inputs.deployment }}/server.json
          git diff --staged --quiet || git commit -m "Deploy ${{ inputs.deployment }} (${{ inputs.provider }}): update state and server config"
          git push

      # Wait for NixOS
      - run: nix develop --command just provider=${{ inputs.provider }} wait ${{ inputs.deployment }}

      # Rebuild with deployment config
      - run: nix develop --command just provider=${{ inputs.provider }} rebuild ${{ inputs.deployment }}

name: Destroy

on:
  workflow_dispatch:
    inputs:
      provider:
        description: 'Cloud provider'
        default: 'vultr'
        type: choice
        options:
          - vultr
          - hetzner
      deployment:
        description: 'Deployment to destroy'
        default: 'default'
        type: choice
        options:
          - default
          - ez3proxy
          - nixginx
          - k3s

env:
  VULTR_API_KEY: ${{ inputs.provider == 'vultr' && secrets.VULTR_API_KEY || '' }}
  HCLOUD_TOKEN: ${{ inputs.provider == 'hetzner' && secrets.HCLOUD_TOKEN || '' }}
  TF_VAR_encryption_passphrase: ${{ secrets.TF_ENCRYPTION_PASSPHRASE }}

jobs:
  destroy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Setup SSH key
        run: |
          mkdir -p keys
          echo "${{ secrets.DEPLOY_KEY }}" > keys/deploy-key
          chmod 600 keys/deploy-key

      # Destroy infrastructure (also removes workspace and server.json)
      - run: nix develop --command just provider=${{ inputs.provider }} destroy ${{ inputs.deployment }}

      - name: Commit state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add terraform/ deployments/${{ inputs.deployment }}/
          git diff --staged --quiet || git commit -m "Destroy ${{ inputs.deployment }} (${{ inputs.provider }}): update state"
          git push
